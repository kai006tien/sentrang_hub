[project]
name = "sen-trang-hub"
version = "0.1.0"
description = "Sen Trắng Hub — System API"
dependencies = [
    "fastapi>=0.111.0",
    "uvicorn>=0.30.0",
    "supabase>=2.5.0",
    "boto3>=1.34.0",
    "pydantic>=2.7.0",
    "pydantic-settings>=2.3.0"
]

[tool.vercel]
entrypoint = "backend.main:app"
